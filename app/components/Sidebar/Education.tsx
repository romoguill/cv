interface EducationProps {
  title: string;
  institution: string;
  date: Date;
}

function Education({ title, institution, date }: EducationProps) {
  return (
    <div>
      <h4></h4>
    </div>
  );
}

export default Education;
